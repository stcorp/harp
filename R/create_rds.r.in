q <- list(
    DESCRIPTION=c(Package="harp", Version="@VERSION@", Built=""),
    Built=list(R=paste(version$major,version$minor,sep="."),
               Platform=version$platform,
               OStype=version$os,
               Date=paste(version$year, version$month, version$day, sep="-"))
)
saveRDS(q, "package.rds")

saveRDS(list(internalsID = .Internal(internalsID())), "features.rds")
